
# Blueprint

## Overview

A simple Flutter application with a theme toggle.

## Features

*   Theme toggle between light and dark mode.
*   `OrdersScreen` as the main screen.
*   Basic project structure with a `features` folder.

## Current Change

*   Created `features/orders/presentation/views/orders_screen.dart`.
*   Updated `app.dart` to use `OrdersScreen` as the home screen.
*   Moved the theme toggle to the `AppBar` in `app.dart`.
